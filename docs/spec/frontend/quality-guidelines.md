# Quality Guidelines

## Status

Not applicable. The repository has no rendered frontend, accessibility tree,
browser tests, or UI lint/build pipeline.

Documentation and workflow presentation changes still require Markdown/YAML
review, `npm run validate:metadata`, `git diff --check`, and consistency with
the public output contract in `DESIGN.md`.
